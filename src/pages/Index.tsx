import SnakeGame from '@/components/SnakeGame';

const Index = () => {
  return (
    <div className="w-full min-h-screen bg-gradient-to-br from-cyan-50 via-cyan-100 to-teal-50 flex items-center justify-center">
      <SnakeGame />
    </div>
  );
};

export default Index;